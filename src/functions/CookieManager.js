// import React, { useEffect } from "react";
// import Cookies from "universal-cookie";

// const CookieManager = ({ onCookieLoad }) => {
//   useEffect(() => {
//     const cookies = new Cookies();
//     const savedValues = cookies.get("alphabetValues");
//     if (savedValues) {
//       onCookieLoad(savedValues);
//     } else {
//       // If no values are found in the cookie, set an empty object
//       onCookieLoad({});
//     }
//   }, [onCookieLoad]);

//   return null;
// };

// export default CookieManager;
